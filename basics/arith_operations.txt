package basics

import "fmt"

func main() {
 
	var a, b int = 10, 5 

	var result int

	result = a + b
	fmt.Println("Addition:", result)

	// signed integeters are +ve and -ve, whilst unsigned are only positive; it can go lowest to zero, but not lower than that
}